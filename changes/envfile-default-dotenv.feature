Set default ``file`` parameter to ``".env"`` in :class:`.EnvFileSource`. The class can now be instantiated without arguments: ``EnvFileSource()`` loads from ``.env`` in the current working directory.
