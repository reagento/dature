Unhandled exceptions out of `dature.load()` no longer print a Python traceback header. Non-dature exceptions raised inside dature (`FileNotFoundError`, parser errors, user `__post_init__` failures) are now wrapped at the `Loader.load()` boundary into `DatureConfigError`, so a single `sys.excepthook` (installed on import) renders them via `traceback.print_exception(..., tb=None)`. The same exception classes raised entirely from user code outside dature are unaffected and still print with the normal traceback.
