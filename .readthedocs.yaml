version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - git fetch --unshallow || true
    - pip install uv
    - uv pip install --system -e ".[yaml,json5,toml,secure]" --group docs
    - python -m mkdocs build --strict --site-dir $READTHEDOCS_OUTPUT/html
