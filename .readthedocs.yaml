version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install uv
    - uv sync --group docs
    - uv pip install -e ".[yaml,json5,toml,secure]"
    - uv run mkdocs build --strict --site-dir $READTHEDOCS_OUTPUT/html
